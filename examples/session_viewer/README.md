## Session Viewer (Sidecar Example)

Minimal Asciinema-like playback of `stdout`/`events` arrays from session fences. Pure JS, offline.

Copy events from fixtures under `doc/fixtures/sessions/` into `player.js`.
